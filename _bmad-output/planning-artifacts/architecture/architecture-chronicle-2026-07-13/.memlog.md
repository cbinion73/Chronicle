---
scope: Cross-device Chronicle handoff through private CloudKit while full working data remains device-local
purpose: Implementation handoff for Apple-platform developers
altitude: feature
updated: 2026-07-13T11:56
---

- (direction) Fast path requested: produce the complete developer handoff without further elicitation.
- (constraint) User-set boundary: every device owns and processes its full local Chronicle information; CloudKit exists only for handoff continuity.
- (decision) Use a local-first bounded handoff projection: local storage is authoritative and CloudKit carries only an expiring resume pointer, preventing CloudKit from becoming a second content database.
- (decision) Use one private custom CloudKit record zone named ChronicleHandoff-v1 and never the public or shared databases, preventing cross-account visibility and record scattering.
- (decision) Model every handoff as an immutable HandoffEnvelope with a UUID record ID and no mutable head record, preventing concurrent devices from overwriting each other.
- (decision) Store only schema metadata, timestamps, source device pseudonym, kind, and an encrypted structured resume payload; exclude notes, prayers, conversations, imported files, audio, OCR, and generated responses.
- (decision) A target resolves local references against its own store and reports source-device-only when absent; it never synthesizes or downloads the missing local object.
- (decision) Use a seven-day retention window and a fifty-envelope cap, with opportunistic expiry pruning, preventing unbounded continuity metadata growth.
- (decision) Use CKSyncEngine change tracking with engine state stored locally per device and a durable local outbox for offline sends, preventing polling and lost offline handoffs.
- (decision) Apply changes in two phases: validate and resolve the envelope first, then mutate only ephemeral navigation state after explicit user acceptance, preventing remote metadata from altering durable local content.
- (version) Verified locally against Xcode 27 beta build 27A5218g, iOS 27.0 SDK, Swift 6, CloudKit private custom zones, CKRecord encryptedValues, and CKSyncEngine APIs.
- (assumption) All participating apps use the same bundle family, CloudKit container, iCloud account, and production schema environment; entitlement activation remains a later implementation step.
- (direction) User clarification supersedes the metadata-only handoff projection: CloudKit is the durable private replica and revision archive; devices remain offline-capable and process records locally.
- (decision) Use a local-first replicated record model: every mutation commits to the device database and durable outbox before UI success, then syncs asynchronously to CloudKit and other devices.
- (decision) Represent each logical object with a stable ChronicleItem head plus immutable ChronicleRevision records; the head selects current state while all prior revisions remain retrievable.
- (decision) Use branch-aware revision ancestry and deterministic merge revisions instead of last-writer-wins, preventing concurrent offline edits from silently erasing either device history.
- (decision) Treat deletion as a synced tombstone revision; retain historical revisions and assets until an explicit user purge policy removes them, preventing one device from resurrecting deleted content.
- (decision) Sync user-owned records and their required assets through the private CloudKit database; keep licensed bundled corpus, caches, model output caches, credentials, and device settings out of CloudKit.
- (decision) Encrypt user content fields with CKRecord encryptedValues and encrypt asset bytes client-side before CKAsset upload, preventing readable private content fields and unencrypted attachment payloads in the CloudKit schema.
- (decision) A handoff is a small ChronicleContinuation record referencing a synced item and revision; targets sync required records first, then resume locally, preventing handoff pointers from outrunning content replication.
- (decision) Use CloudKit native encryption: encryptedValues for private record payloads and CKAsset default encryption for files; do not add custom cryptography in v1, preventing an unreviewed key-recovery system.
- (decision) Sync kinds are Chronicle entries, prayers, formation rhythms, bookmarks, memory verses, owned-book metadata and study work, AI thread history selected for retention, and user-imported assets; device UI state, credentials, voice configuration, caches, bundled corpora, and ephemeral model output remain local-only.
- (decision) Every CloudKit entity uses deterministic stable item identifiers and immutable revision UUIDs; timestamps are UTC Date values and payloads carry an explicit content schema version.
- (decision) A device may compact local history but CloudKit revisions remain retrievable by item and ancestry; permanent purge is a distinct synced command requiring explicit user confirmation.
- (decision) Reviewer correction: item heads are a deterministic set of maximal revision tips, not one current pointer; tombstones dominate visibility until a resolution descends from every branch.
- (decision) Reviewer correction: partition SQLite and all sync state by CloudKit account identity; account changes stop sync and never rebind pending records automatically.
- (decision) Reviewer correction: RFC 8785 canonical JSON, checked-in per-kind JSON Schemas, and shared golden fixtures are mandatory across Swift and TypeScript.
- (decision) Reviewer correction: use SQLite as the device database, atomicByZone CloudKit change batches, ordered dependency confirmation, and no outbox revision coalescing.
- (decision) Reviewer correction: revision payloads cap at 512 KiB; large state shards by logical unit or required CKAsset and schemas exclude device paths.
- (version) CKSyncEngine availability corrected to iOS 17.0+ after local Xcode 27 SDK inspection.
- (event) Independent reviewer gate completed; critical and high data-integrity, technology, and convergence findings incorporated.
- (event) spine finalized
